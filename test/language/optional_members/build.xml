<!--

Ant build.xml for optional_members test example.

The following properties can be set:

zserio.release_dir      - Directory where to find Zserio release to test. Default is "../../../distr".
test.build_dir          - Directory where to put generated files. Default is "../../../build/test".
findbugs.home_dir       - Location of the findbugs tool. If not set, findbugs is not run.

-->
<project name="optional_members" basedir="." default="run">
    <dirname property="optional_members.base_dir" file="${ant.file.optional_members}"/>

    <property name="zserio.root_dir" location="${optional_members.base_dir}/../../.."/>
    <property name="zserio.release_dir" location="${zserio.root_dir}/distr"/>
    <property name="zserio.jar_dir" location="${zserio.release_dir}/zserio_libs"/>

    <property name="test.build_dir" location="${zserio.root_dir}/build/test"/>
    <property name="runtime.jar_dir" location="${zserio.release_dir}/runtime_libs/java"/>
    <property name="3rdparty.jar_dir" location="${zserio.root_dir}/3rdparty/java"/>

    <property name="optional_members.build_dir"
        location="${test.build_dir}/language/optional_members"/>
    <property name="optional_members.zs.in_dir" location="${optional_members.base_dir}/zs"/>
    <property name="optional_members.zs.out_dir" location="${optional_members.build_dir}/gen"/>
    <property name="optional_members.run_dir" location="${optional_members.build_dir}/run"/>

    <property name="runtime.jar_file" location="${runtime.jar_dir}/zserio_runtime.jar"/>
    <property name="3rdparty.junit.jar_file" location="${3rdparty.jar_dir}/junit-4.10.jar"/>

    <!-- Zserio Ant task -->
    <taskdef name="zserio" classpath="${zserio.release_dir}/ant_task/zserio_ant.jar" classname="zserio.ant.ZserioTask"/>

    <!-- classpath required for running the Zserio Tool -->
    <path id="zserio.classpath">
        <fileset dir="${zserio.jar_dir}">
            <include name="*.jar"/>
            <exclude name="zserio_ant.jar"/>
        </fileset>
    </path>

    <target name="prepare">
        <mkdir dir="${optional_members.build_dir}/classes"/>
    </target>

    <target name="gen" depends="prepare">
        <zserio srcPath="${optional_members.zs.in_dir}" srcFile="optional_members.zs"
            java="${optional_members.zs.out_dir}">
            <arg name="withWriterCode"/>
            <dependencies>
                <fileset dir="${optional_members.zs.in_dir}" includes="**/*.zs"/>
                <fileset dir="${zserio.jar_dir}"/>
                <fileset dir="${runtime.jar_dir}"/>
                <fileset file="${ant.file.optional_members}"/>
            </dependencies>
            <output>
                <fileset dir="${optional_members.zs.out_dir}" includes="**/*.java" erroronmissingdir="false"/>
            </output>
            <classpath>
                <path refid="zserio.classpath"/>
            </classpath>
        </zserio>
    </target>

    <target name="compile" depends="gen">
        <depend srcDir="${optional_members.base_dir}/java:${optional_members.zs.out_dir}"
            destDir="${optional_members.build_dir}/classes" cache="${optional_members.build_dir}/depend-cache"/>
        <javac destdir="${optional_members.build_dir}/classes" debug="on" encoding="utf8"
            includeAntRuntime="false">
            <classpath>
                <pathelement location="${runtime.jar_file}"/>
                <pathelement location="${3rdparty.junit.jar_file}"/>
            </classpath>
            <src location="${optional_members.base_dir}/java"/>
            <src location="${optional_members.zs.out_dir}"/>
        </javac>
    </target>

    <target name="findbugs" depends="compile" if="findbugs.home_dir" description="Run FindBugs">
        <taskdef name="findbugs" classpath="${findbugs.home_dir}/lib/findbugs-ant.jar"
            classname="edu.umd.cs.findbugs.anttask.FindBugsTask"/>
        <findbugs home="${findbugs.home_dir}"
            output="html"
            outputFile="${optional_members.build_dir}/findbugs.html"
            reportLevel="low"
            errorProperty="optional_members.is_failed"
            warningsProperty="optional_members.is_failed">
            <sourcePath>
                <dirset dir="${optional_members.base_dir}/java"/>
                <dirset dir="${optional_members.zs.out_dir}"/>
            </sourcePath>
            <fileset dir="${optional_members.build_dir}/classes"/>
            <auxClasspath>
                <fileset file="${runtime.jar_file}"/>
                <fileset file="${3rdparty.junit.jar_file}"/>
            </auxClasspath>
        </findbugs>
        <fail message="FindBugs found some issues!" if="optional_members.is_failed"/>
    </target>

    <target name="run" depends="findbugs">
        <delete dir="${optional_members.run_dir}"/>
        <mkdir dir="${optional_members.run_dir}"/>
        <junit printsummary="no" fork="on" haltonfailure="on" dir="${optional_members.run_dir}">
            <classpath>
                <pathelement location="${optional_members.build_dir}/classes"/>
                <pathelement location="${runtime.jar_file}"/>
                <pathelement location="${3rdparty.junit.jar_file}"/>
            </classpath>
            <batchtest>
                <fileset dir="${optional_members.base_dir}/java" includes="**/*Test.java"/>
                <formatter type="plain" usefile="false"/>
            </batchtest>
        </junit>
    </target>

    <target name="clean">
        <delete dir="${optional_members.build_dir}"/>
    </target>
</project>
