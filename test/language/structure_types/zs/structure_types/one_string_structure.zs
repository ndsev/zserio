package structure_types.one_string_structure;

// one string structure was disabled for Java in the past because of clash between file name constructor and
// field constructor
struct OneStringStructure
{
    string  oneString;
};
